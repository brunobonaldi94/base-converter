import React from 'react';
import {
  HeaderContainer,
  Nav,
} from './styles';

function Header() {
  return (
    <HeaderContainer>
      <Nav>
        Header
      </Nav>
    </HeaderContainer>
  );
}

export default Header;
